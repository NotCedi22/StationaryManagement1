@model StationaryManagement.Models.Role

@{
    ViewData["Title"] = "Role Details";
}

<h1>Role Details</h1>

<div class="card p-3 mb-3">
    <dl class="row">
        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.RoleName)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.RoleName)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Description)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Description)</dd>
    </dl>
</div>

<a asp-action="Edit" asp-route-id="@Model.RoleId" class="btn btn-warning">Edit</a>
<a asp-action="Index" class="btn btn-secondary ms-2">Back to List</a>
